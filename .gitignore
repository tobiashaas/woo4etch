# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local working files (Perplexity export etc.)
*.local.md
*.draft.md
.firecrawl/

# Local helper scripts (not part of the repo)
_*.py

# Node / build artifacts (if you later add tooling)
node_modules/
dist/
build/
.cache/

# Environment
.env
.env.local

# wp-env local test environment (machine-specific absolute paths)
.wp-env.json
.wp-env.override.json
.wp-env/

# Test artifacts (screenshots / generated output; the .php + .sh tooling stays tracked)
tests/manual/*.png
tests/manual/*.log
